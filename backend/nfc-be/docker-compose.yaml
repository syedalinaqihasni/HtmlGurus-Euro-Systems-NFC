version: '3.8'

services:
  app-nfc-be:
    build: .
    restart: on-failure
    ports:
      - "8080:8080"
    environment:
      # Port & Env
      PORT: 8080
      NODE_ENV: development

      # MongoDB
      MONGODB_URI: mongodb+srv://owaisshaikh:arpZnejZNYT0hVGg@the-cluster.i2mvxmw.mongodb.net/?retryWrites=true&w=majority&appName=the-cluster

      # JWT
      JWT_SECRET: kP9!zX7$wT3@vQ1!eL6^mR8*oJ2&bD4@fH5+uA0$cN3%yW9!sT
      JWT_EXPIRES_IN: 7d

      # AWS S3
      S3_SECRET_KEY: UqdcdItzysNouHM6lacoC9gWRMCa2APomp5ZvILg
      S3_BUCKET: espark-nfc-project
      S3_REGION: ap-south-1
      S3_ENDPOINT: s3.ap-south-1.amazonaws.com

      # Email / SMTP
      SMTP_HOST: smtp.office365.com
      SMTP_PORT: 587
      EMAIL_USER: sapb1@eurosystems.com
      EMAIL_PASS: "Caz99435%"
      EMAIL_FROM: sapb1@eurosystems.com
      EMAIL_FROM_NAME: "NFC Workforce Manager"

      # Super Admin Secret
      SUPER_ADMIN_CREATION_SECRET: 9m!R@7$wLp*Zt38Q&cUv%BqHdE&x2pLs
